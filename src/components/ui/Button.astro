---
interface Props {
    href?: string
    variant?: "primary" | "outline"
    class?: string
}

const { href, variant = "primary", class: className = "" } = Astro.props

const baseClasses =
    "inline-flex items-center gap-2 font-body font-medium text-sm tracking-wide transition-all duration-300 rounded-full"
const variants = {
    primary:
        "bg-accent text-bg-primary hover:bg-accent-hover px-7 py-3.5 hover:shadow-lg hover:shadow-accent/20",
    outline:
        "border border-accent-line text-accent hover:bg-accent-subtle px-7 py-3.5",
}

const classes = `${baseClasses} ${variants[variant]} ${className}`
const Tag = href ? "a" : "button"
---

<Tag href={href} class={classes}>
    <slot />
</Tag>
